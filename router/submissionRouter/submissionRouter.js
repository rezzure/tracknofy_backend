// const express = require('express');
// const router = express.Router();
// const submissionController = require('../../controllers/submissionControlForm/submissionControlForm');
// const upload = require('../../middleware/dynamicForm.multer/multer');

// router.get('/', submissionController.getAllSubmissions);
// router.get('/:id/submissions', submissionController.getFormSubmissions);
// router.post('/:id/submit', upload.array('images', 10), submissionController.submitForm);

// module.exports = router;